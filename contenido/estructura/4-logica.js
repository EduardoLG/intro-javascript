/** Operadores logicos
 * 
 * 
 *  AND, OR y NOT - Y, o, NEGACION (NO)
 *  && operador Y
 *  || operador O
 *  ! operador de negacion
 */

const a = 10
const b = 20
const c = '10'

(a == b && a === c)
(a != b || a === c)
!(a === c)